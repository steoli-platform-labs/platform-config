apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sample-api-production
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/steoli-platform-labs/helm-charts.git
    targetRevision: main
    path: charts/sample-api
    helm:
      releaseName: sample-api
      values: |
        image:
          repository: ghcr.io/steoli-platform-labs/sample-api
          tag: 1.0.0
        environment: production
        replicaCount: 2
        autoscaling:
          minReplicas: 2
          maxReplicas: 5
        secret:
          enabled: true
          remoteKey: platform-labs/sample-api-production
  destination:
    server: https://kubernetes.default.svc
    namespace: sample-api-production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
